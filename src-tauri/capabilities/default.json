{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for overlay and settings windows",
  "windows": ["overlay", "settings", "widget"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "core:tray:default",
    "allow-minimal-mode"
  ]
}
